## What does this PR do?

<!-- One or two sentences. Link an issue if there is one: Closes #123 -->

## Type of change

- [ ] Bug fix
- [ ] New feature
- [ ] Breaking change (existing behavior changes)
- [ ] Documentation only
- [ ] Refactor / no behavior change

## Checklist

- [ ] I ran `npm test` in `backend/` and it passes
- [ ] I ran `npm run build` in `frontend/` and it succeeds
- [ ] I tested this manually against a running `docker compose up` stack
- [ ] If this touches authorization/RBAC, I added or updated a test in
      `backend/tests/` — see `docs/ARCHITECTURE.md` for how the
      project-scoped role checks work before changing them
- [ ] If this touches the database schema, I updated `docs/DATABASE_SCHEMA.md`
- [ ] If this touches an API endpoint, I updated `docs/API_DOCUMENTATION.md`
- [ ] No secrets, API keys, or `.env` files are included in this diff

## Screenshots (for UI changes)

<!-- Drag images in here if this changes anything visual -->
